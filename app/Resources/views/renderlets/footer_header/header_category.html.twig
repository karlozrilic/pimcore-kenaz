<ul>
{% for category in categories %}
    <li>
        <a href="{{path(category)}}" 
            id="{{category.getKey()|lower}}"
            {% if path(category) == current_url %}class="active"{% endif %}>
            {{category.getTitle}}
        </a>
    </li>
{% endfor %}
</ul>